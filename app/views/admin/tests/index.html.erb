<% provide(:title, 'Tests') %>
<h1> <%= t('.header') %> </h1>

<p> <%= link_to "New Test", new_admin_test_path %> </p>

<table>
  <tr>
    <th><%=t('.title') %></th>
    <th><%=t('.level') %></th>
    <th><%=t('.count') %></th>
    <th colspan="3"><%=t('.actions') %></th>


  </tr>

  <%= render @tests %>

</table>
