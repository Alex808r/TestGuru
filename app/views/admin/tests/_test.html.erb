<tr>
  <td><%= test.title %></td>
  <td><%= test_level(test) %></td>
  <td><%= test.questions.count %></td>

  <td> <%= link_to t('shared.links.show'), admin_test_path(test) %> </td>
  <td> <%= link_to t('shared.links.edit'), edit_admin_test_path(test) %> </td>
  <td> <%= link_to t('shared.links.delete'), admin_test_path(test), method: :delete,
       data: { confirm: t('shared.delete_confirmation') } %> </td>
</tr>