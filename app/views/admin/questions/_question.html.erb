

<tr>
  <td> <%= question.id %></td>
  <td> <%= question.body %></td>
  <td> <%= link_to t('shared.links.show'), admin_question_path(question) %> </td>
  <td> <%= link_to t('shared.links.edit'), edit_admin_question_path(question) %> </td>
  <td> <%= link_to t('shared.links.delete'), admin_question_path(question), method: :delete,
       data: { confirm: t('shared.delete_confirmation') } %> </td>
</tr>


