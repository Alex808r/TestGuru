<h2 class="display-4"><%= t('.header') %></h2>

<table class="table table-hover">
  <tr class="table-primary">
    <th class="align-middle"><%= t('.title') %></th>
    <th class="align-middle"><%= t('.image_url') %></th>
    <th class="align-middle"><%= t('.date') %></th>
  </tr>
  <%= render current_user.badges.order(created_at: :desc)  %>
</table>

<p>
  <%= link_to t('shared.links.back'), root_path %>
</p>

<%= render 'badges/all_badges'%>
