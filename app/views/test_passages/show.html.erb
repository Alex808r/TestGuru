<% provide(:title, "Passing") %>

<h1> <%= t('.header', title: @test_passage.test.title) %> </h1>

<h3> <%= t('.total_questions', count: @test_passage.test.questions.size) %> </h3>

<h3> <%= t('.current_question', number: @test_passage.current_question_number) %></h3><br>


  <p>
    <b>  <%= @test_passage.current_question.body %> </b>
  </p>

<%= form_with url: test_passage_path(@test_passage), local: true, method: :put do |form| %>
  <%= form.collection_check_boxes :answer_ids, @test_passage.current_question.answers, :id, :body, include_hidden: false do |b| %>
  <p>
    <%= b.check_box id: "answer_ids_#{b.object_id}" %>
    <%= b.label %>
  </p>
  <% end %>
  <p> <%= link_to "Gist Question", gist_test_passage_path(@test_passage), method: :post %></p>
  <p> <%= form.submit t('shared.button.next') %> </p>
<% end %>